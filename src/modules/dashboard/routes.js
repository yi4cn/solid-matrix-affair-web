import DashboardPage from './pages/dashboard-page.vue';

const routes = [
    {
        path: '/dashboard/',
        name: 'dashboard',
        component: DashboardPage,
    }
];

export default routes;